## filename=${context.javaBeanName}Resp, folder=
package ${package}.${moduleName}.resp;

import lombok.Data;

import java.time.LocalDateTime;
#if(${hasBigDecimal})
import java.math.BigDecimal;
#end

/**
 * ${tableComment} - 响应
 *
 * @author ${author}
 * @date ${datetime}
 */
@Data
public class ${context.javaBeanName}Resp {
    
    /**
     * 主键ID
     */
    private Long id;
    
#foreach ($column in $columns)
#if($column.columnName != 'id' && $column.columnName != 'del_flag')
    /**
     * ${column.comments}
     */
    private ${column.attrType} ${column.attrName};
    
#end
#end
}