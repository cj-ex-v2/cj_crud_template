## filename=${context.javaBeanName}Resp.java, folder=resp
import lombok.Data;

/**
 * ${table.tableName} ${table.comment}
 *
 * ${context.date} ${context.time}
 */
@Data
public class ${context.javaBeanName}Resp {
    
    
#foreach ($column in $columns)
#if($column.columnName != 'del_flag')
    /**
     * ${column.comment}
     */
    private ${column.javaTypeBox} ${column.javaFieldName};
    
#end
#end
}